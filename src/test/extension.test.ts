/**
 * TODO: fix these tests
 */
// The module 'assert' provides assertion methods from node
import * as assert from 'assert';
// import * as vscode from 'vscode';
// import * as ToggleFormatOnSave from '../extension';

// You can import and use all API from the 'vscode' module
// as well as import your extension to test it
// import * as vscode from 'vscode';
// import * as myExtension from '../extension';

// Defines a Mocha test suite to group tests of similar kind together
suite("Toggle FormatOnSave Tests", function () {

    // Defines a Mocha unit test
    test("FormatOnSave activates", function () {
        assert.equal(-1, [1, 2, 3].indexOf(5));
        assert.equal(-1, [1, 2, 3].indexOf(0));
        // const config = v scode.workspace.getConfiguration('editor');
        // assert.equal(false, config.get('formatOnSave'));

        // ToggleFormatOnSave.activate({});
        
        
    });
});